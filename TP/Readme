# Projet 231 : Opérations sur les listes chaînées

## Description
Ce TP regroupe plusieurs algorithmes classiques et opérations sur les **listes chaînées** (simples, doublement, circulaires).  
L’utilisateur interagit via un menu et peut choisir parmi différentes fonctionnalités :  

- Insertion en position quelconque dans une liste simple triée  
- Insertion en position quelconque dans une liste doublement triée  
- Suppression des occurrences dans une liste chaînée simple  
- Insertion en tête et en fin dans une liste chaînée (simplement et doublement) circulaire  

---

## Organisation des fichiers
| Fichier | Description |
|---------|-------------|
| `fichier.h` | Fichier d’en-tête qui contient toutes les déclarations des fonctions. |
| `fichier.c` | Fichier source qui contient l’implémentation des fonctions. |
| `main.c` | Programme principal qui propose un menu interactif pour tester les fonctions. |

---

## Fonctionnalités disponibles

| Opération | Description |
|-----------|-------------|
| Suppression des occurrences dans une liste chaînée simple | Supprimer la valeur qui se répète dans la liste |
| Insertion en position dans une liste simple triée | Insérer une valeur en tête, en fin ou au milieu dans une liste simple triée |
| Insertion en position dans une liste double triée | Insérer une valeur en tête, en fin ou au milieu dans une liste doublement triée |
| Insertion en tête dans une liste simplement circulaire | Insérer une valeur en tête de liste dans une liste chaînée simplement circulaire (dernier nœud pointe vers la tête) |
| Insertion en fin dans une liste simplement circulaire | Insérer une valeur en fin de liste dans une liste chaînée simplement circulaire (dernier nœud pointe vers la tête) |
| Insertion en tête dans une liste doublement chaînée circulaire | Insérer une valeur en tête de liste dans une liste chaînée doublement circulaire (dernier nœud pointe vers la tête) |
| Insertion en fin dans une liste doublement circulaire | Insérer une valeur en fin de liste dans une liste chaînée doublement circulaire (dernier nœud pointe vers la tête) |
| Vider la liste avant toute autre opération | Vider la liste pour éviter le mélange des opérations sur les listes chaînées |
| Quitter | Sort du programme |

---

## Compilation et exécution
```bash
gcc main.c fichier.c -o programme
./main
